import 'package:flutter/material.dart';

/// AudioMark brand colour (matches the splash background and launcher icon).
const kPrimaryColor = Color.fromARGB(255, 223, 96, 96);

/// Shown in Settings › About. Keep in sync with pubspec.yaml `version:`.
const kAppVersion = '2.1.0';
